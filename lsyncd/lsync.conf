settings {
   nodaemon   = true,
}

sync{default.rsync,
        source="/data",
        target="pi@lodge:/data",
	delete=false,
	exclude={ "incoming.data" },
	delay=5,
        rsync = {
		archive=true,
                compress=false,
		binary="/app/wrapper"
        }
}
